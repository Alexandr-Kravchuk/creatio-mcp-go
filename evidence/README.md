# Evidence output

`latest.json` is generated by `scripts/compare-with-clio.sh` and ignored by Git. It contains only the parity verdict, row counts, SHA-256 fingerprints of canonicalized result sets, and mismatch counts. It must never contain a URL, stand name, credentials, token, or an installed-application value.
